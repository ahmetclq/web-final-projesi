@model SwapSmart.Models.Item
@{
    ViewData["Title"] = "Takas Teklifi Gönder";
    var requestedItem = ViewBag.RequestedItem as SwapSmart.Models.Item;
    var userItems = ViewBag.UserItems as List<SwapSmart.Models.Item>;
}

<div class="row justify-content-center">
    <div class="col-md-10 col-lg-8">
        <div class="card shadow-sm mb-4">
            <div class="card-body p-4">
                <h4 class="card-title mb-3">Teklif Verilen İlan</h4>
                @if (requestedItem != null)
                {
                    <div class="row">
                        <div class="col-md-4">
                            @if (requestedItem.Images.Any())
                            {
                                <img src="@requestedItem.Images.First().ImagePath" class="img-fluid rounded" alt="@requestedItem.Title" />
                            }
                            else
                            {
                                <div class="bg-light d-flex align-items-center justify-content-center rounded" style="height: 200px;">
                                    <span class="text-muted">Fotoğraf Yok</span>
                                </div>
                            }
                        </div>
                        <div class="col-md-8">
                            <h5>@requestedItem.Title</h5>
                            <p class="text-muted">@requestedItem.Description</p>
                            <div class="mb-2">
                                <strong>Değer:</strong> ₺@requestedItem.EstimatedMinValue.ToString("N0") - ₺@requestedItem.EstimatedMaxValue.ToString("N0")
                            </div>
                            <div class="mb-2">
                                <span class="badge bg-info">İstenen: @requestedItem.WantedItemName</span>
                            </div>
                            <div>
                                <span class="badge bg-secondary">📍 @requestedItem.City, @requestedItem.District</span>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>

        <div class="card shadow-sm">
            <div class="card-body p-5">
                <h2 class="card-title text-center mb-4">Takas Teklifi Gönder</h2>
                
                <form asp-action="Send" method="post">
                    <input type="hidden" name="requestedItemId" value="@requestedItem?.Id" />
                    
                    <div class="mb-4">
                        <label for="offeredItemId" class="form-label fw-bold">Teklif Ettiğiniz İlanınız</label>
                        <select name="offeredItemId" id="offeredItemId" class="form-select" required>
                            <option value="">İlanınızı Seçin</option>
                            @if (userItems != null)
                            {
                                @foreach (var item in userItems)
                                {
                                    <option value="@item.Id">@item.Title (₺@item.EstimatedMinValue.ToString("N0") - ₺@item.EstimatedMaxValue.ToString("N0"))</option>
                                }
                            }
                        </select>
                        <small class="form-text text-muted">Takas etmek istediğiniz ilanınızı seçin.</small>
                    </div>
                    
                    <div class="alert alert-info mb-4">
                        <strong>ℹ️ Bilgi:</strong> Teklif gönderildikten sonra, ilan sahibi teklifinizi kabul veya reddedebilir.
                    </div>
                    
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">Teklifi Gönder</button>
                        <a asp-action="Detail" asp-controller="Item" asp-route-id="@requestedItem?.Id" class="btn btn-outline-secondary">İptal</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
